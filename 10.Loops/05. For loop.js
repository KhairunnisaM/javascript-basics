
// syntax of while loop


/*let initial=0;  // starting initialization

while(condition)      // loop condition
{

    initial++ ;        // updation
}

// Syntax of for loop

for(initialization; condition; updation)*/

for(let start=0; start<=10; start++){
    console.log(start)

}

let tableNumber= 5;

for(let startValue= 1; startValue<=10; startValue++)
{
    console.log(`${tableNumber} x ${startValue} = ${tableNumber * startValue}`)
}